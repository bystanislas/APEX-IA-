services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: apex
      POSTGRES_PASSWORD: apex
      POSTGRES_DB: apex_ia
    ports:
      - "5432:5432"
    volumes:
      - apex_ia_postgres:/var/lib/postgresql/data

volumes:
  apex_ia_postgres:
